\begin{proof}
    TBD. Without \Cref{ass:exog} the eigenvalue homogeneity result \Cref{lem:bnh} fails, so the secular equation does not collapse to the two-eigenvalue form. Argument follows along the lines of the proof of \Cref{prop:tlr-lim} but without the simplification step.
\end{proof}
